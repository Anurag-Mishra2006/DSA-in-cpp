#include <bits/stdc++.h>
using namespace std;

int main() {
    //  the case of underflow rise when 
    //  if in an empty stack , and we try to perform 2 function:
                    //  st.pop()
                    //  st.top ()
    stack<int>st;
    // cout<<st.pop();
    
    return 0;
}